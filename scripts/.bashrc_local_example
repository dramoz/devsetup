# ----------------------------------------------------------------
# Env. Variables
#export DUMMY_ENV=value
#export LICENSE_FILE=path_to_license

# ----------------------------------------------------------------
# Tools paths
#export TOOLS_PATH="/data/tools"
export TOOLS_PATH="${HOME}/tools"
export PATH=${TOOLS_PATH}:$PATH

# --------------------------------
# RISC-V
# https://github.com/riscv-collab/riscv-gnu-toolchain
# https://www.sifive.com/software (pre-build)
#export PATH=/opt/riscv64-unknown-elf-toolchain/bin:$PATH

# --------------------------------
# Ruby Gems: install Ruby Gems to ~/gems
#export GEM_HOME="$HOME/gems"
#export PATH="$HOME/gems/bin:$PATH"

# --------------------------------
# OSS CAD Suite (formely FPGA-Toolchain / OpenSource)
# https://github.com/YosysHQ/oss-cad-suite-build
#export PATH="${TOOLS_PATH}/oss-cad-suite/bin:$PATH"

# --------------------------------
# Antlr
# https://www.antlr.org/
#export CLASSPATH=".:/usr/local/lib/antlr-4.10.1-complete.jar:$CLASSPATH"
#alias antlr4='java -jar /usr/local/lib/antlr-4.10.1-complete.jar'
#alias grun='java org.antlr.v4.gui.TestRig'
